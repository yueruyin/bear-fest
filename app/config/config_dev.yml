app:
  env: dev
  title: "Bear Fest Backend"
  version: "0.1.0"

server:
  host: "0.0.0.0"
  port: 8000

database:
  url: "sqlite:///./app.db"

uploads:
  root_dir: "app/uploads"
  merchant_dir: "merchant"

cors:
  allow_origins: ["*"]
  allow_credentials: true

